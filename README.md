# ProjectTemplate

シンプルな Spring Boot テンプレート（Thymeleaf + Spring Security + H2）

実行方法（Windows PowerShell）:

```pwsh
./gradlew bootRun
```

デフォルトユーザ:
- ユーザ名: `user`
- パスワード: `password`
# SpringProjectTemplates
